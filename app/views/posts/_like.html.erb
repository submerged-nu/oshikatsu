<%= turbo_frame_tag dom_id(post, :likes) do %>
  <% if current_user.likes.exists?(post: post) %>
    <%= button_to 'いいねを取り消す', like_path(post), method: :delete %>
  <% else %>
    <%= button_to 'いいね', likes_path(post_id: post.id), method: :post %>
  <% end %>
<% end %>